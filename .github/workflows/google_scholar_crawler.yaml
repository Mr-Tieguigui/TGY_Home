name: Get Citation Data

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Citation crawler disabled
        run: echo "Google Scholar crawler is disabled for this site."
